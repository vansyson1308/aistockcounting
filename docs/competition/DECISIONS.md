# Decisions log

Format: ID, date, decision, why, and what it deviates from (if anything).

| ID | Date | Decision | Why / deviation |
|---|---|---|---|
| D-001 | 2026-09-25 | `docs/competition/SPEC.md` was authored from the mission brief. | The mission names SPEC.md as the source of truth, but it did not exist in the repo. §4.4 (policy), §7 (gates) and §9 (video) are therefore defined by us from the brief. |
| D-002 | 2026-09-25 | Work happens on `claude/relaxed-wozniak-nhlyvm`, not `opencv-comp`. | The session harness only allows pushes to its designated branch. This branch plays the role of `opencv-comp`. The owner can rename it on GitHub (`git push origin claude/relaxed-wozniak-nhlyvm:opencv-comp`). |
| D-003 | 2026-09-25 | Football-pivot directories (`ml/`, `reports/gate0a/`, `tools/camsim/`) stay in the tree until the owner approves their removal (Checkpoint A item 3). | Removal needs owner approval, and creating a `football-pivot` branch means pushing to a branch outside the session's allowed set. The competition runtime does not import them, and the license gate treats them as a separate surface. |
| D-004 | 2026-09-25 | Pinned `opencv-python-headless==5.0.0.93` (verified on PyPI 2026-09-25; manylinux x86_64 and aarch64 wheels exist). | Required by the rules. The headless build avoids GUI/X11 libraries in the container. |
| D-005 | 2026-09-25 | Budget "step" = one controller decision that runs perception tools. The terminal action (accept / recapture / escalate + `render_evidence`) is not a step. `rectify_tray` + `detect` share one step. | Lets the hardest demo path (assess → count → tile → zoom → compare → escalate) fit in the mandated 5 steps while still tracing every tool call individually. See SPEC §4.4. |
| D-006 | 2026-09-25 | Detector backends: `onnx` (YOLOX via `cv2.dnn`, the product path), `classical` (OpenCV-only highlight/contour baseline, no learned weights) and `mock` (tests only). Mock is used only when `DETECTOR_BACKEND=mock` or `MOCK_MODE=true` is set explicitly. | The mission requires mock mode to be explicit only. The classical baseline keeps the whole agent demo-able before weights exist, and is labelled as such in `/health` and in every trace. It is never reported as the product model. |
| D-007 | 2026-09-25 | HTTPS via CloudFront in front of the Graviton instance. | This gives a valid TLS certificate without owning a domain or running certbot. It is inside the free tier for demo traffic. |
| D-008 | 2026-09-25 | `compare_previous` uses ORB, with SIFT as a fallback, instead of AKAZE. | In the OpenCV 5.0.0 main wheel, `cv2.AKAZE_create` / `KAZE` are not present (checked with `dir(cv2)`; the modules built are calib, core, dnn, features, flann, geometry, …). ORB and SIFT are present. Deviation from the brief's "ORB/AKAZE". |
| D-009 | 2026-09-25 | Face blur uses `cv2.FaceDetectorYN` with a vendored YuNet ONNX (MIT, 232 KB). There is no Haar fallback. | The OpenCV 5 main wheel ships no cascade data (`cv2/data` is empty) and no `CascadeClassifier`. YuNet is the OpenCV-native detector. |
| D-010 | 2026-09-25 | The backend base image is `public.ecr.aws/docker/library/python:3.11-slim-bookworm` (ARG `BASE_IMAGE`), with no apt layer. The healthcheck is Python urllib. | Public ECR avoids Docker Hub rate limits on EC2. Locally (this sandbox) the image is built with `--build-arg BASE_IMAGE=mirror.gcr.io/library/python:3.11-slim-bookworm --secret id=ca,...` because Docker Hub returned 429 and the ECR blob CDN is blocked by the sandbox proxy. |
| D-011 | 2026-09-25 | `alembic/env.py` now uses the async engine pattern and reads `DATABASE_URL`. The image entrypoint runs `alembic upgrade head`. | The previous env.py used a sync engine with an asyncpg URL and a hard-coded host, so migrations could not run in the container. Verified 0001→0004, down to 0003 and back up, on Postgres 16. |
| D-012 | 2026-09-25 | Runtime requirements are split from dev (`backend/requirements-dev.txt`). `onnxruntime` and `ultralytics` are removed from the runtime. | The runtime image carries only what production needs. OpenCV 5 DNN runs the model. |
| D-013 | 2026-09-25 | The approval gate is enforced in code. `awaiting_approval` blocks `PATCH /review`, `POST /discrepancies/{id}/resolve` and `POST /scans/{id}/agent-run` (409). Only `POST /scans/{id}/approve`, which requires `approver_id`, moves the scan on. `auto_accept` is only allowed when count == POS (no inventory change) or when there is no POS figure (the scan stays `pending_review`). | SPEC §8: a human approves any count that changes inventory. The planner cannot bypass this, because `allowed_actions` omits `auto_accept` unless its conditions hold. |
| D-014 | 2026-09-25 | The live timeline uses an in-process progress registry (`app/agent/progress.py`) polled through `GET /scans/{id}/trace`. uvicorn runs 1 worker. | Simpler than SSE or Redis for a single Graviton host. OpenCV releases the GIL, so the CV work runs in threads. `agent_steps` stays the source of truth. |
| D-015 | 2026-09-25 | CloudWatch metrics are published with `PutMetricData` (best effort, background thread), not EMF. | This works with any log driver and with the instance-role permission `cloudwatch:PutMetricData`. |
| D-016 | 2026-09-25 | `compare_previous` sets its minimum changed-region size from the detected item size (bounding-box area, 0.3 × the median box), after close-then-open morphology. | A fixed threshold missed small or ring-shaped removals (a missing ring leaves a thin, hollow diff). |
| D-017 | 2026-09-25 | New guard: when the tiled pass finds fewer items than the single shot in any cell (a "view conflict"), auto-accept is not allowed, even if a zoom later brings the count back to the POS figure. | Found while exporting the trace demo. A glare blob was a false single-shot item; tiling dropped it (correctly); zoom re-confirmed it, so the count equalled a stale POS figure and was auto-accepted. The count was wrong. Views of the same tray that disagree now go to a human. Covered by a regression test. |
