{
  "name": "trayagent-infra",
  "private": true,
  "description": "Pins the AWS CDK CLI used to synth/deploy the TrayAgent stack (the stack itself is Python).",
  "scripts": {
    "cdk": "cdk"
  },
  "devDependencies": {
    "aws-cdk": "2.1143.0"
  }
}
