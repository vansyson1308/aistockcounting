# Agentic evaluation: pending real photos

`RESULTS.md`, `results.csv`, `summary.json`, the plots and `failures/` are written here by
`make eval-agent` (`training/scripts/agent_eval.py`). It only runs against the frozen, verified
real test split. Nothing here comes from synthetic data. The folder stays empty until the
owner's photos are labelled (BLOCKERS B-001).
